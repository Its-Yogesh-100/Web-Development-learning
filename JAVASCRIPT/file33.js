// for of loop 


// one also way to iterate the loop is
const arr=[10,20,30,40,50];

// this loop will run until the length of arr array

for(let array of arr){

    console.log(array);
}

const ap=[];
for(let k of arr){

    ap.push(arr.toUpperCase());

}
console.log(ap);